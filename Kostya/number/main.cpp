#include "header.h"
#include "numberClass.cpp"

int main (int ac, **char av[])
{
	if (ac == 2)
	{
		for (int i = 0; i <= strlen(av[]); i++)
		{
			cout << av[i];
			if (av[i] == ' ') cout << "\n";
		}
	}
	return 0;
}